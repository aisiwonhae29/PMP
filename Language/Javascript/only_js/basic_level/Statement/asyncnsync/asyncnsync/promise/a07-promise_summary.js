/**
 * * a07: summary of promise
 * 
 * - promise is object that has cycle which has pending, fulfill || reject, settled.
 * - await handle about value of promise object's fulfillment.
 * - in settled state, there is then native object to control this return value
 * - async function make promise object automatically and return fulfilled value. In contrast normal function return promise obejct.
 * 
 */