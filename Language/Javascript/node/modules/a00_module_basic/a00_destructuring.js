/**
 * * a00: destructuring
 * 
 * ! about
 * 
 * - Destructuring syntax extracts the inside specified obeject into variable based object { 'a', 'b' ...}
 */

function t01(){
    let winston = require('winston');
    const { combine, timestamp, json } = winston.format;
}

