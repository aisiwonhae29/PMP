package com.example.intermediate.lang;

public class process_t {
    /**
     * ! Process
     * 
     * - This represents a native process created by the java app.
     * - It provides a way to interact with and control external processes that are spawned by the Java program.
     * 
     * # feature
     * 
     * * 1. Process representation
     * 
     * - Represents an external process initiated by the Java app.
     * - Created using Runtime.exec() or ProcessBuilder.start()
     * 
     * * 2. Process Interaction
     * 
     * - Allows interaction with the external process through IO streams.
     * - Provides methods to monitor and control the process lifecycle.
     */
}
