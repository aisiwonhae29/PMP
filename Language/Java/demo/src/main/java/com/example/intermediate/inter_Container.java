package com.example.intermediate;

import com.example.intermediate.sequence.synchronize;
import com.example.intermediate.process.Thread_t;

public class inter_Container {
    public synchronize syn = new synchronize();
    public Thread_t thread = new Thread_t(12, "siwon");
}
