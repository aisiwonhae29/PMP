var app = require('../app');
var debug = require('debug')('express-work:server');
var http = require('http');

var port = nomalizePort(process.env.PORT || '3000');
app.set('port', port);

var server = http.createServer(app);